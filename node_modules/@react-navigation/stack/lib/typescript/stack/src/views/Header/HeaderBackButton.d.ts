import * as React from 'react';
import { StackHeaderLeftButtonProps } from '../../types';
declare type Props = StackHeaderLeftButtonProps & {
    tintColor: string;
};
declare type State = {
    fullLabelWidth?: number;
};
declare class HeaderBackButton extends React.Component<Props, State> {
    static defaultProps: {
        pressColorAndroid: string;
        tintColor: string;
        labelVisible: boolean;
        truncatedLabel: string;
    };
    state: State;
    private handleLabelLayout;
    private shouldTruncateLabel;
    private renderBackImage;
    private renderLabel;
    private handlePress;
    render(): JSX.Element;
}
export default HeaderBackButton;
