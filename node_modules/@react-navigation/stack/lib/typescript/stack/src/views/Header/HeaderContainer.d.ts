/// <reference types="react" />
import { StyleProp, ViewStyle } from 'react-native';
import { Route } from '@react-navigation/core';
import { StackNavigationState } from '@react-navigation/routers';
import { EdgeInsets } from 'react-native-safe-area-context';
import { Layout, Scene, StackHeaderStyleInterpolator } from '../../types';
export declare type Props = {
    mode: 'float' | 'screen';
    layout: Layout;
    insets: EdgeInsets;
    scenes: Array<Scene<Route<string>> | undefined>;
    state: StackNavigationState;
    getPreviousRoute: (props: {
        route: Route<string>;
    }) => Route<string> | undefined;
    onContentHeightChange?: (props: {
        route: Route<string>;
        height: number;
    }) => void;
    styleInterpolator: StackHeaderStyleInterpolator;
    style?: StyleProp<ViewStyle>;
};
export default function HeaderContainer({ mode, scenes, layout, insets, state, getPreviousRoute, onContentHeightChange, styleInterpolator, style, }: Props): JSX.Element;
