/// <reference types="react" />
import { TextProps } from 'react-native';
declare type Props = TextProps & {
    children?: string;
};
export default function HeaderTitle({ style, ...rest }: Props): JSX.Element;
export {};
