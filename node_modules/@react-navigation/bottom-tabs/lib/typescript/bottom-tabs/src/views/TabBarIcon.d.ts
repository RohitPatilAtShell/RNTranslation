import React from 'react';
import { StyleProp, ViewStyle } from 'react-native';
import { Route } from '@react-navigation/core';
declare type Props = {
    route: Route<string>;
    size: number;
    activeOpacity: number;
    inactiveOpacity: number;
    activeTintColor: string;
    inactiveTintColor: string;
    renderIcon: (props: {
        route: Route<string>;
        focused: boolean;
        color: string;
        size: number;
    }) => React.ReactNode;
    style: StyleProp<ViewStyle>;
};
export default function TabBarIcon({ route, activeOpacity, inactiveOpacity, activeTintColor, inactiveTintColor, renderIcon, size, style, }: Props): JSX.Element;
export {};
