import * as React from 'react';
import { TabNavigationState } from '@react-navigation/routers';
import { BottomTabNavigationConfig, BottomTabDescriptorMap, BottomTabNavigationHelpers } from '../types';
declare type Props = BottomTabNavigationConfig & {
    state: TabNavigationState;
    navigation: BottomTabNavigationHelpers;
    descriptors: BottomTabDescriptorMap;
};
declare type State = {
    loaded: number[];
};
export default class BottomTabView extends React.Component<Props, State> {
    static defaultProps: {
        lazy: boolean;
    };
    static getDerivedStateFromProps(nextProps: Props, prevState: State): {
        loaded: number[];
    };
    state: {
        loaded: number[];
    };
    private renderButton;
    private renderIcon;
    private getLabelText;
    private getAccessibilityLabel;
    private getAccessibilityRole;
    private getAccessibilityStates;
    private getTestID;
    private handleTabPress;
    private handleTabLongPress;
    private renderTabBar;
    render(): JSX.Element;
}
export {};
